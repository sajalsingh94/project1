# Environment Configuration
# Copy this file to .env and update the values as needed

# Server Configuration
PORT=3001

# MongoDB Configuration (Optional)
# Uncomment ONE of the following options:

# Option 1: MongoDB Atlas (Cloud) - Recommended for production
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=cluster

# Option 2: Local MongoDB - For local development
# MONGODB_URI=mongodb://localhost:27017/bihari_delicacies

# MongoDB Database Name (optional, defaults to 'bihari_delicacies')
# MONGODB_DB=bihari_delicacies

# Note: If no MONGODB_URI is provided, the application will use JSON file storage
# This is perfectly fine for development and testing!